{
  "name": "raffler",
  "version": "2",
  "description": "Raffler App",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "clear-dist": "rm -rf dist",
    "dev": "npm run clear-dist && parcel src/*.html --no-source-maps --no-cache",
    "build": "npm run clear-dist && parcel build src/*.html --no-source-maps --no-content-hash"
  },
  "author": "drementer",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drementer/raffler.git"
  },
  "bugs": {
    "url": "https://github.com/drementer/raffler/issues"
  },
  "homepage": "https://github.com/drementer/raffler#readme",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.9",
    "parcel": "^2.12.0",
    "postcss": "^8.4.47",
    "svgo": "^3.3.2",
    "tailwindcss": "^3.4.14"
  }
}
